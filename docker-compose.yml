services:
  ostinato-radio:
    build: .
    container_name: ostinato-radio
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
    # The backend binds to 0.0.0.0 so it's reachable from the WiiM on the LAN
    network_mode: host
